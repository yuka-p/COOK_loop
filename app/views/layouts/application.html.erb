<!DOCTYPE html>
<html data-theme="mytheme">
  <head>
    <title><%= content_for(:title) || "Myapp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <link href="https://fonts.googleapis.com/css2?family=Kiwi+Maru:wght@300&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen">

    <!-- Header -->
    <header class="bg-[#90755a] shadow-md">
      <div class="flex items-center justify-between px-4 h-16">
      <link href="https://fonts.googleapis.com/css2?family=Poiret+One&display=swap" rel="stylesheet">

        <!-- ロゴ（左・ホームリンク付き） -->
        <div>
          <%= link_to root_path, class: "flex items-center gap-2" do %>
            <%= image_tag "icon.png", alt: "COOK loop", class: "h-10 w-10 object-contain" %>

            <span class="text-white text-3xl tracking-widest"
                  style="font-family: 'Poiret One', sans-serif;">
              COOK loop
            </span>
          <% end %>
        </div>

        <!-- ハンバーガーメニュー（右） -->
        <div class="dropdown dropdown-end">
          <label tabindex="0" class="btn btn-ghost btn-circle text-white hover:bg-white/20">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
                 class="inline-block w-6 h-6 stroke-current">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
          </label>

          <ul tabindex="0"
              class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52">

            <li><%= link_to "使い方", how_to_path %></li>
            <li><%= link_to "利用規約", terms_path %></li>
            <li><%= link_to "プライバシーポリシー", privacy_path %></li>
            <li>設定</li>

            <% if user_signed_in? %>
              <li>
                <%= button_to "ログアウト",
                  destroy_user_session_path,
                  method: :delete,
                  data: { confirm: "ログアウトしますか？" },
                  class: "logout-button" %>
              </li>
            <% else %>
              <li><%= link_to "ログイン", new_user_session_path %></li>
            <% end %>
          </ul>
        </div>

      </div>
    </header>

    <!-- Flash messages -->
    <% if notice.present? %>
      <div class="bg-[#5a906a] text-white max-w-xl mx-auto mt-4 px-4 py-3 rounded shadow-lg">
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="bg-[#905a65] text-white max-w-xl mx-auto mt-4 px-4 py-3 rounded shadow-lg">
        <%= alert %>
      </div>
    <% end %>

    <!-- Main content -->
    <main class="p-4">
      <%= yield %>
    </main>
    <footer class="mt-16 border-t border-base-200 bg-[#90755a]">
      <div class="max-w-5xl mx-auto px-4 py-6 text-center text-sm text-white space-y-2">

        <div class="flex justify-center gap-6">
          <%= link_to "利用規約", terms_path, class: "hover:underline" %>
          <%= link_to "プライバシーポリシー", privacy_path, class: "hover:underline" %>
        </div>

        <div>
          © 2026 COOK loop
        </div>
      </div>
    </footer>
  </body>
</html>
